server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EnzoFerreira4152/config-data-files.git
          #default-label: develop
          #search-paths: config-data